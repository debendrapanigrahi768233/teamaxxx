import { makeStyles } from "@mui/styles";

const ChatbotStyles = makeStyles((theme) => ({
  kanbanBoardWrapper: {
    height:'100vh',
    width: '100vw',
    backgroundColor:"blue",
  },
  kanbanBoardSubWrapper:{
    width:"100%",
    height:"100%"
  }
    
}));

export default ChatbotStyles;